version: '3.7'

services:
    ssh:
        build:
            context: ./ssh
            args:
                SSH_USER: ${SSH_USER}
                SSH_PASS: ${SSH_PASS}
        ports:
            - "2222:22"